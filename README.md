# Video Subtitle Generator

แอปพลิเคชันสำหรับแปลงไฟล์วิดีโอเป็นซับไตเติ้ลหลายภาษาด้วย AI

## ✨ Features
- 📹 อัปโหลดไฟล์วิดีโอ (MP4, MOV, AVI, MKV, WMV)
- 🎵 แปลงเป็น MP3 และดาวน์โหลดได้
- 🎤 ใช้ OpenAI Whisper ASR สำหรับแกะเสียงเป็นข้อความพร้อม timestamp
- 🎬 **แก้ไข Subtitle พร้อมดูวิดีโอ**: แก้ไข subtitle แต่ละรายการพร้อมเล่นวิดีโอประกอบ
  - Video player ที่ sync กับ subtitle
  - คลิกที่ timestamp เพื่อเล่นวิดีโอจากจุดนั้น
  - Subtitle ที่กำลังเล่นจะถูกไฮไลท์อัตโนมัติ
  - แก้ไขข้อความแต่ละ segment ได้ง่าย
- 🌍 แปลเป็นภาษาต่างๆ: อังกฤษ, ลาว, พม่า, กัมพูชา, เวียดนาม
- 🎨 กำหนด style การแปลได้
- 📄 ดาวน์โหลดไฟล์ SRT ทุกภาษา
- 🎞️ ฝัง subtitle เข้ากับวิดีโอ (Hard/Soft subtitle)

## 🛠 Tech Stack
- **Frontend**: React + Vite + Tailwind CSS
- **Backend**: FastAPI (Python)
- **AI**: OpenAI Whisper + GPT-4o-mini
- **Video Processing**: MoviePy

## 🚀 Quick Start

### 1. Setup
```bash
# Clone และเข้าไปในโฟลเดอร์
git clone <repository-url>
cd video-subtitle-generator

# รัน setup script
./setup.sh
```

### 2. ตั้งค่า OpenAI API Key
```bash
# แก้ไขไฟล์ .env
nano .env

# เพิ่ม API key
OPENAI_API_KEY=your_openai_api_key_here
```

### 3. รันแอปพลิเคชัน

**Terminal 1 - Backend:**
```bash
./start-backend.sh
```

**Terminal 2 - Frontend:**
```bash
./start-frontend.sh
```

### 4. เข้าใช้งาน
- 🌐 Frontend: http://localhost:3000
- 🔧 Backend API: http://localhost:8000

## 📋 Manual Setup (ถ้า script ไม่ทำงาน)

### Backend Setup
```bash
cd backend
python3 -m venv venv
source venv/bin/activate  # On Windows: venv\Scripts\activate
pip install -r requirements.txt
python main.py
```

### Frontend Setup
```bash
cd frontend
npm install
npm run dev
```

## 🎯 การใช้งาน

1. **อัปโหลดวิดีโอ**: ลากไฟล์วิดีโอมาวางหรือคลิกเพื่อเลือก
2. **รอการแปลง**: ระบบจะแปลงเป็น MP3 อัตโนมัติ
3. **แกะเสียง**: ใช้ OpenAI Whisper แกะเสียงเป็นข้อความพร้อม timestamp
4. **แก้ไข Subtitle**: แก้ไข subtitle แต่ละรายการพร้อมดูวิดีโอประกอบ
   - เล่นวิดีโอและดู subtitle ที่กำลังเล่น
   - คลิกที่ timestamp เพื่อข้ามไปยังจุดนั้นในวิดีโอ
   - แก้ไขข้อความที่ผิดพลาดหรือไม่ถูกต้อง
   - บันทึกการแก้ไขก่อนไปขั้นตอนถัดไป
5. **แปลภาษา**: เลือกภาษาที่ต้องการและกำหนด style
6. **ดาวน์โหลด**: ดาวน์โหลดไฟล์ SRT ทุกภาษา
7. **ฝัง Subtitle**: ฝัง subtitle เข้ากับวิดีโอ (Hard/Soft)

## 🔧 API Endpoints

- `POST /upload-video` - อัปโหลดไฟล์วิดีโอ
- `GET /download-mp3/{file_id}` - ดาวน์โหลด MP3
- `POST /transcribe/{file_id}` - แกะเสียง
- `GET /stream-video/{file_id}` - Stream วิดีโอสำหรับ video player
- `POST /update-srt/{file_id}` - อัปเดต SRT ที่แก้ไขแล้ว
- `POST /translate` - แปลภาษา
- `GET /download-srt/{file_id}/{language}` - ดาวน์โหลด SRT
- `POST /embed-subtitles` - ฝัง subtitle เข้ากับวิดีโอ
- `GET /download-video/{file_id}/{language}/{subtitle_type}` - ดาวน์โหลดวิดีโอที่ฝัง subtitle

## 📝 Requirements

- Python 3.8+
- Node.js 16+
- OpenAI API Key
- FFmpeg (สำหรับ MoviePy)