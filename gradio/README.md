# Gradio Interface for Video Subtitle Generator

Gradio UI สำหรับ Video Subtitle Generator ที่ใช้ Backend API เดียวกันกับ Frontend

## ✨ Features

- 🔐 **Authentication**: Login/Logout system
- 📹 **Upload Video**: อัปโหลดและแปลงวิดีโอเป็น MP3
- 🎤 **Transcription**: แกะเสียงด้วย OpenAI หรือ Botnoi
- ✏️ **Edit Subtitles**: แก้ไข subtitle ในรูปแบบตาราง
- 🌍 **Translation**: แปลเป็นหลายภาษา (อังกฤษ, ลาว, พม่า, กัมพูชา, เวียดนาม)
- 📥 **Download**: ดาวน์โหลดไฟล์ SRT
- 🎞️ **Embed Subtitles**: ฝัง subtitle เข้ากับวิดีโอ (Hard/Soft)
- ⚙️ **Font Customization**: ปรับแต่งฟอนต์สำหรับ hard subtitle

## 🚀 Quick Start

### 1. ติดตั้ง Dependencies

```bash
cd gradio
pip install -r requirements.txt
```

### 2. ตั้งค่า Environment

```bash
# Copy .env.example to .env
cp .env.example .env

# แก้ไข .env ถ้าต้องการเปลี่ยน API URL
nano .env
```

### 3. รัน Backend (ต้องรันก่อน)

```bash
# ใน terminal แยก
cd ../backend
source venv/bin/activate
python main.py
```

### 4. รัน Gradio App

```bash
# กลับมาที่ gradio folder
cd ../gradio
python app.py
```

### 5. เข้าใช้งาน

เปิดเบราว์เซอร์ไปที่: http://localhost:7860

## 📋 การใช้งาน

### 1. เข้าสู่ระบบ
- กรอก username และ password
- กด "เข้าสู่ระบบ"

### 2. อัปโหลดวิดีโอ (Tab 1)
- เลือกไฟล์วิดีโอ
- กด "อัปโหลด"
- รอให้ระบบแปลงเป็น MP3

### 3. แกะเสียง (Tab 2)
- เลือกผู้ให้บริการ (OpenAI หรือ Botnoi)
- กด "เริ่มแกะเสียง"
- ดูผลการแกะเสียง

### 4. แก้ไข Subtitle (Tab 3)
- แก้ไขข้อความในตาราง
- กด "บันทึกการแก้ไข"

### 5. แปลภาษา (Tab 4)
- เลือกภาษาเป้าหมาย
- เลือกผู้ให้บริการ
- (ไม่บังคับ) กรอก Style Prompt
- กด "แปลภาษา"
- ดาวน์โหลดไฟล์ SRT

### 6. ฝัง Subtitle (Tab 5)
- เลือกภาษา
- เลือกประเภท (Hard/Soft)
- ปรับแต่งฟอนต์ (สำหรับ Hard subtitle)
- กด "ฝัง Subtitle"
- ดาวน์โหลดวิดีโอ

## 🔧 Configuration

### API URL
แก้ไขใน `.env`:
```
API_URL=http://localhost:8000
```

หรือตั้งค่าผ่าน environment variable:
```bash
export API_URL=http://your-backend-url:8000
python app.py
```

### Port
แก้ไขใน `app.py` (บรรทัดสุดท้าย):
```python
app.launch(
    server_name="0.0.0.0",
    server_port=7860,  # เปลี่ยน port ที่นี่
    share=False
)
```

### Share Mode
ถ้าต้องการ public URL (Gradio share link):
```python
app.launch(
    server_name="0.0.0.0",
    server_port=7860,
    share=True  # เปลี่ยนเป็น True
)
```

## 📝 ความแตกต่างจาก Frontend

### ข้อดี
- ✅ ติดตั้งง่ายกว่า (ไม่ต้อง npm install)
- ✅ รันได้เลยด้วย Python
- ✅ UI สำเร็จรูปจาก Gradio
- ✅ รองรับ share mode (public URL)

### ข้อจำกัด
- ⚠️ Video player ไม่มี sync กับ subtitle แบบ real-time
- ⚠️ การแก้ไข subtitle เป็นแบบตาราง (ไม่มี video preview)
- ⚠️ UI ไม่ customizable เท่า React frontend

## 🆚 เปรียบเทียบกับ Frontend

| Feature | Frontend (React) | Gradio |
|---------|-----------------|--------|
| Authentication | ✅ | ✅ |
| Upload Video | ✅ | ✅ |
| Transcription | ✅ | ✅ |
| Edit Subtitles | ✅ (with video sync) | ✅ (table only) |
| Translation | ✅ | ✅ |
| Download SRT | ✅ | ✅ |
| Embed Subtitles | ✅ | ✅ |
| Font Customization | ✅ | ✅ |
| Video Player Sync | ✅ | ❌ |
| Installation | npm install | pip install |
| Customization | High | Limited |

## 🐛 Troubleshooting

### Backend ไม่ทำงาน
```bash
# ตรวจสอบว่า backend รันอยู่หรือไม่
curl http://localhost:8000

# ถ้าไม่ทำงาน ให้รัน backend
cd backend
source venv/bin/activate
python main.py
```

### Connection Error
- ตรวจสอบ `API_URL` ใน `.env`
- ตรวจสอบว่า backend รันที่ port 8000
- ตรวจสอบ firewall

### Authentication Error
- ตรวจสอบ username/password
- ตรวจสอบว่า backend มี user ในระบบ
- สร้าง user ใหม่ด้วย `create_user.py`

## 📚 เอกสารเพิ่มเติม

- [Backend API Documentation](../backend/main.py)
- [Gradio Documentation](https://www.gradio.app/docs)
- [Project README](../README.md)
