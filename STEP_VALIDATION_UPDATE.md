# การปรับปรุงระบบ Step Validation

## การเปลี่ยนแปลง

### 1. ลบข้อมูลความยาววิดีโอรวมออกจาก Quota Display

**เดิม:**
```
## 📊 Quota การใช้งาน

🎬 จำนวนวิดีโอ: 3/10 ไฟล์ (30.0%)  
🟢 เหลือเยอะ (เหลือ 7 ไฟล์)

⏱️ ความยาววิดีโอรวม: 4.5/10 นาที (45.0%)  
🟢 เหลือ 5.5 นาที

📏 ขนาดไฟล์สูงสุด: 500 MB ต่อไฟล์
```

**ใหม่:**
```
## 📊 Quota การใช้งาน

🎬 จำนวนวิดีโอ: 3/10 ไฟล์ (30.0%)  
🟢 เหลือเยอะ (เหลือ 7 ไฟล์)

📏 ขนาดไฟล์สูงสุด: 500 MB ต่อไฟล์
```

**เหตุผล:** ทำให้ UI กระชับและเน้นข้อมูลที่สำคัญที่สุด

---

### 2. เพิ่ม Step Validation Logic

ผู้ใช้ต้องทำ step ก่อนหน้าให้เสร็จก่อนถึงจะเข้า step ถัดไปได้

#### Step Flow:

```
1️⃣ อัปโหลดวิดีโอ
    ↓ (upload สำเร็จ)
2️⃣ แกะเสียง (ปลดล็อค)
    ↓ (transcribe สำเร็จ)
3️⃣ แก้ไข Subtitle (ปลดล็อค)
    ↓ (บันทึกการแก้ไขสำเร็จ)
4️⃣ แปลภาษา (ปลดล็อค)
    ↓ (แปลสำเร็จ)
5️⃣ ฝัง Subtitle (ปลดล็อค)
```

---

## การทำงานของระบบ

### Tab 1: อัปโหลดวิดีโอ
- **สถานะเริ่มต้น:** เปิดใช้งานได้ทันที
- **เมื่อ upload สำเร็จ:** ปลดล็อค Tab 2

### Tab 2: แกะเสียง
- **สถานะเริ่มต้น:** แสดงข้อความ "⚠️ กรุณาอัปโหลดวิดีโอที่ Tab 1 ก่อน"
- **หลัง upload วิดีโอ:** ซ่อนข้อความเตือน, แสดงเนื้อหา Tab 2
- **เมื่อ transcribe สำเร็จ:** ปลดล็อค Tab 3

### Tab 3: แก้ไข Subtitle
- **สถานะเริ่มต้น:** แสดงข้อความ "⚠️ กรุณาแกะเสียงที่ Tab 2 ก่อน"
- **หลัง transcribe:** ซ่อนข้อความเตือน, แสดงเนื้อหา Tab 3
- **เมื่อบันทึกการแก้ไขสำเร็จ:** ปลดล็อค Tab 4

### Tab 4: แปลภาษา
- **สถานะเริ่มต้น:** แสดงข้อความ "⚠️ กรุณาบันทึกการแก้ไข Subtitle ที่ Tab 3 ก่อน"
- **หลังบันทึก subtitle:** ซ่อนข้อความเตือน, แสดงเนื้อหา Tab 4
- **เมื่อแปลสำเร็จ:** ปลดล็อค Tab 5

### Tab 5: ฝัง Subtitle
- **สถานะเริ่มต้น:** แสดงข้อความ "⚠️ กรุณาแปลภาษาที่ Tab 4 ก่อน (หรือใช้ subtitle ต้นฉบับ)"
- **หลังแปลภาษา:** ซ่อนข้อความเตือน, แสดงเนื้อหา Tab 5

---

## Implementation Details

### 1. เพิ่ม Warning Messages

แต่ละ Tab (ยกเว้น Tab 1) มี warning message:

```python
# Tab 2
step2_warning = gr.Markdown("⚠️ **กรุณาอัปโหลดวิดีโอที่ Tab 1 ก่อน**", visible=True)

# Tab 3
step3_warning = gr.Markdown("⚠️ **กรุณาแกะเสียงที่ Tab 2 ก่อน**", visible=True)

# Tab 4
step4_warning = gr.Markdown("⚠️ **กรุณาบันทึกการแก้ไข Subtitle ที่ Tab 3 ก่อน**", visible=True)

# Tab 5
step5_warning = gr.Markdown("⚠️ **กรุณาแปลภาษาที่ Tab 4 ก่อน (หรือใช้ subtitle ต้นฉบับ)**", visible=True)
```

### 2. เพิ่ม Content Containers

แต่ละ Tab มี content container ที่ซ่อนไว้:

```python
# Tab 2
transcribe_tab_content = gr.Column(visible=False)

# Tab 3
edit_tab_content = gr.Column(visible=False)

# Tab 4
step4_content = gr.Column(visible=False)

# Tab 5
step5_content = gr.Column(visible=False)
```

### 3. Event Handlers ที่ปลดล็อค Steps

#### Upload → Unlock Step 2
```python
def upload_and_unlock_step2(video_file):
    """Upload video and unlock step 2"""
    result = upload_video(video_file)
    return result + (gr.update(visible=False),)  # Hide step2_warning

upload_btn.click(
    upload_and_unlock_step2,
    inputs=[video_input],
    outputs=[..., step2_warning]
)
```

#### Transcribe → Unlock Step 3
```python
def transcribe_and_prepare_edit(provider):
    """Transcribe and prepare edit tab"""
    # ... transcribe logic ...
    if success:
        return (..., gr.update(visible=False))  # Hide step3_warning
    else:
        return (..., gr.update(visible=True))   # Keep step3_warning

transcribe_btn.click(
    transcribe_and_prepare_edit,
    inputs=[provider_transcribe],
    outputs=[..., step3_warning]
)
```

#### Save Subtitle → Unlock Step 4
```python
def save_and_unlock_step4(edited_text):
    """Save subtitles and unlock step 4"""
    status, text = save_edited_subtitles(edited_text)
    if "✅" in status:
        return status, text, gr.update(visible=False), gr.update(visible=True)
    else:
        return status, text, gr.update(visible=True), gr.update(visible=False)

save_all_btn.click(
    save_and_unlock_step4,
    inputs=[subtitle_editor],
    outputs=[edit_status, subtitle_editor, step4_warning, step4_content]
)
```

#### Translate → Unlock Step 5
```python
def translate_and_unlock_step5(language, style, provider):
    """Translate and unlock step 5"""
    status, file = translate_subtitles(language, style, provider)
    if "✅" in status:
        return status, file, gr.update(visible=False), gr.update(visible=True)
    else:
        return status, file, gr.update(visible=True), gr.update(visible=False)

translate_btn.click(
    translate_and_unlock_step5,
    inputs=[language_select, style_input, provider_translate],
    outputs=[translate_status, srt_file, step5_warning, step5_content]
)
```

---

## ประโยชน์

### 1. User Experience ที่ดีขึ้น
- ✅ ผู้ใช้รู้ว่าต้องทำอะไรก่อน
- ✅ ไม่สับสนว่าจะเริ่มต้นที่ไหน
- ✅ มี flow ที่ชัดเจน

### 2. ป้องกัน Error
- ✅ ไม่สามารถ transcribe ก่อน upload
- ✅ ไม่สามารถแปลก่อนมี subtitle
- ✅ ไม่สามารถฝังก่อนมี subtitle

### 3. UI ที่กระชับขึ้น
- ✅ ลบข้อมูลความยาววิดีโอที่ไม่จำเป็น
- ✅ เน้นข้อมูลที่สำคัญ (จำนวนไฟล์, ขนาดไฟล์)

---

## การทดสอบ

### Test Case 1: ลำดับการทำงานปกติ
1. Login
2. Upload วิดีโอ → Tab 2 ปลดล็อค
3. Transcribe → Tab 3 ปลดล็อค
4. แก้ไขและบันทึก subtitle → Tab 4 ปลดล็อค
5. แปลภาษา → Tab 5 ปลดล็อค
6. ฝัง subtitle

**ผลลัพธ์ที่คาดหวัง:** ทุก step ทำงานได้ตามลำดับ

### Test Case 2: พยายามข้าม Step
1. Login
2. ไปที่ Tab 2 โดยไม่ upload

**ผลลัพธ์ที่คาดหวัง:** แสดงข้อความ "⚠️ กรุณาอัปโหลดวิดีโอที่ Tab 1 ก่อน"

### Test Case 3: Upload ล้มเหลว
1. Login
2. พยายาม upload ไฟล์ใหญ่เกิน limit

**ผลลัพธ์ที่คาดหวัง:** 
- แสดง error message
- Tab 2 ยังคงล็อคอยู่

### Test Case 4: Transcribe ล้มเหลว
1. Upload วิดีโอสำเร็จ
2. Transcribe แต่เกิด error

**ผลลัพธ์ที่คาดหวัง:**
- แสดง error message
- Tab 3 ยังคงล็อคอยู่

### Test Case 5: บันทึก Subtitle ล้มเหลว
1. Upload และ transcribe สำเร็จ
2. พยายามบันทึก subtitle แต่เกิด error

**ผลลัพธ์ที่คาดหวัง:**
- แสดง error message
- Tab 4 ยังคงล็อคอยู่

---

## Checklist

### UI Changes
- [x] ลบข้อมูลความยาววิดีโอรวมออกจาก quota display
- [x] เพิ่ม warning messages ในแต่ละ tab
- [x] เพิ่ม content containers ที่ซ่อนไว้

### Logic Changes
- [x] Upload → unlock Tab 2
- [x] Transcribe → unlock Tab 3
- [x] Save subtitle → unlock Tab 4
- [x] Translate → unlock Tab 5

### Error Handling
- [x] Upload ล้มเหลว → Tab 2 ยังล็อค
- [x] Transcribe ล้มเหลว → Tab 3 ยังล็อค
- [x] Save ล้มเหลว → Tab 4 ยังล็อค
- [x] Translate ล้มเหลว → Tab 5 ยังล็อค

---

## ไฟล์ที่แก้ไข

- `gen_subtitle/gradio/app.py`
  - ปรับปรุง `format_quota_display()` - ลบข้อมูลความยาววิดีโอ
  - เพิ่ม warning messages ในแต่ละ tab
  - เพิ่ม content containers
  - เพิ่ม wrapper functions สำหรับ unlock steps
  - ปรับปรุง event handlers

---

## หมายเหตุ

- Tab 5 (ฝัง Subtitle) สามารถใช้ subtitle ต้นฉบับได้ ไม่จำเป็นต้องแปลภาษา
- Warning messages จะซ่อนอัตโนมัติเมื่อทำ step ก่อนหน้าสำเร็จ
- ถ้า action ล้มเหลว warning จะยังคงแสดงอยู่
- ระบบตรวจสอบความสำเร็จจากข้อความที่มี "✅" ใน status
