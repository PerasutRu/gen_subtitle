# การปรับปรุงระบบ Quota และ Usage Tracking

## สิ่งที่ปรับปรุง

### 1. ปรับปรุงฟังก์ชัน `format_quota_display()`

เพิ่มการแสดงข้อมูลที่ละเอียดขึ้น:

```python
def format_quota_display(refresh_usage=False):
    """Format quota information for display
    
    Args:
        refresh_usage: ถ้าเป็น True จะดึงข้อมูล usage ใหม่จาก API
    """
```

**ข้อมูลที่แสดง:**
- 🎬 จำนวนวิดีโอที่ upload แล้ว / สูงสุด (เปอร์เซ็นต์)
- 📊 สถานะ: เหลือเยอะ / ใกล้เต็ม / เต็มแล้ว
- 📝 จำนวนไฟล์ที่เหลือ
- ⏱️ ความยาววิดีโอรวม / สูงสุด (เปอร์เซ็นต์)
- ⏰ เวลาที่เหลือ (นาที)
- 📏 ขนาดไฟล์สูงสุดต่อไฟล์

**ตัวอย่างการแสดงผล:**
```
## 📊 Quota การใช้งาน

🎬 จำนวนวิดีโอ: 3/10 ไฟล์ (30.0%)  
🟢 เหลือเยอะ (เหลือ 7 ไฟล์)

⏱️ ความยาววิดีโอรวม: 4.5/10 นาที (45.0%)  
🟢 เหลือ 5.5 นาที

📏 ขนาดไฟล์สูงสุด: 500 MB ต่อไฟล์
```

### 2. เพิ่มฟังก์ชัน `refresh_quota()`

ฟังก์ชันใหม่สำหรับดึงข้อมูล usage ล่าสุดจาก API:

```python
def refresh_quota():
    """Refresh quota display by fetching latest usage from API"""
    try:
        if not session.session_id:
            return "⚠️ ยังไม่มี session กรุณา upload วิดีโอก่อน"
        
        # Fetch latest usage from API
        usage = get_session_usage()
        
        if usage is None:
            return "❌ ไม่สามารถดึงข้อมูล usage ได้"
        
        # Format and return updated quota display
        return format_quota_display(refresh_usage=False)
        
    except Exception as e:
        print(f"Error refreshing quota: {e}")
        return f"❌ เกิดข้อผิดพลาด: {str(e)}"
```

### 3. เพิ่มปุ่ม "🔄 รีเฟรช Quota" ใน UI

ผู้ใช้สามารถกดปุ่มนี้เพื่อ update ข้อมูล quota ได้ตลอดเวลา:

```python
with gr.Row():
    refresh_quota_btn = gr.Button("🔄 รีเฟรช Quota", variant="secondary", size="sm", scale=1)
    logout_btn = gr.Button("🚪 ออกจากระบบ", variant="stop", size="sm", scale=1)
```

### 4. Auto-update Usage หลัง Upload

ปรับปรุงฟังก์ชัน `upload_video()` ให้ update usage อัตโนมัติหลัง upload สำเร็จ:

```python
if response.status_code == 200:
    result = response.json()
    # ... save session data ...
    
    # Update usage from API response
    session.usage = result.get('usage', {})
    
    # Refresh quota display with updated usage
    quota_text = format_quota_display(refresh_usage=False)
```

### 5. ดึง Usage ตอน Login

ปรับปรุงฟังก์ชัน `login()` ให้ดึง usage ถ้ามี session อยู่แล้ว:

```python
# Get limits after login
get_limits()

# Try to get existing session usage if available
if session.session_id:
    get_session_usage()

quota_text = format_quota_display()
```

## API Endpoints ที่ใช้

### 1. GET `/limits`
ดึงข้อมูล limits ของระบบ:
```json
{
  "maxVideos": 10,
  "maxDurationMinutes": 10,
  "maxFileSizeMB": 500,
  "allowedExtensions": [".mp4", ".mov", ".avi", ".mkv", ".wmv"]
}
```

### 2. GET `/session/{session_id}/usage`
ดึงข้อมูล usage ของ session:
```json
{
  "usage": {
    "videos_count": 3,
    "total_duration": 270.5,
    "remaining_videos": 7,
    "remaining_duration": 329.5
  },
  "limits": {
    "maxVideos": 10,
    "maxDurationMinutes": 10,
    "maxFileSizeMB": 500
  }
}
```

### 3. POST `/upload-video`
Response รวมข้อมูล usage ที่ update แล้ว:
```json
{
  "file_id": "...",
  "session_id": "...",
  "usage": {
    "videos_count": 4,
    "total_duration": 360.0,
    "remaining_videos": 6,
    "remaining_duration": 240.0
  }
}
```

## การทำงานของระบบ

### Flow การ Update Usage:

1. **Login** → ดึง limits และ usage (ถ้ามี session)
2. **Upload Video** → Backend คำนวณและส่ง usage ใหม่กลับมา
3. **Manual Refresh** → ผู้ใช้กดปุ่ม "รีเฟรช Quota" เพื่อ update

### Status Indicators:

| เปอร์เซ็นต์ | Icon | สถานะ |
|------------|------|-------|
| 0-69%      | 🟢   | เหลือเยอะ |
| 70-89%     | 🟡   | ใกล้เต็ม |
| 90-100%    | 🔴   | เต็มแล้ว! |

## ประโยชน์

1. ✅ **ข้อมูลแม่นยำ** - ดึงข้อมูลจาก API โดยตรง
2. ✅ **Real-time Update** - Update อัตโนมัติหลัง upload
3. ✅ **Manual Refresh** - ผู้ใช้สามารถ refresh ได้ตลอดเวลา
4. ✅ **ข้อมูลละเอียด** - แสดงทั้งจำนวนไฟล์และความยาววิดีโอ
5. ✅ **Visual Indicators** - ใช้สีและไอคอนแสดงสถานะ
6. ✅ **คำนวณที่เหลือ** - แสดงจำนวนที่เหลือให้ชัดเจน

## การทดสอบ

### Test Case 1: Login ครั้งแรก
1. Login เข้าระบบ
2. ตรวจสอบว่าแสดง quota: 0/10 ไฟล์

### Test Case 2: Upload วิดีโอ
1. Upload วิดีโอ 1 ไฟล์
2. ตรวจสอบว่า quota update เป็น: 1/10 ไฟล์
3. ตรวจสอบว่าแสดงความยาววิดีโอรวม

### Test Case 3: Manual Refresh
1. กดปุ่ม "🔄 รีเฟรช Quota"
2. ตรวจสอบว่าข้อมูล update

### Test Case 4: Multiple Uploads
1. Upload วิดีโอหลายไฟล์
2. ตรวจสอบว่า quota เพิ่มขึ้นทุกครั้ง
3. ตรวจสอบว่าสถานะเปลี่ยนเมื่อใกล้เต็ม (🟢 → 🟡 → 🔴)

### Test Case 5: Session Persistence
1. Upload วิดีโอ
2. Logout
3. Login อีกครั้ง
4. ตรวจสอบว่า quota ยังคงอยู่ (ถ้า session ยังไม่หมดอายุ)

## ไฟล์ที่แก้ไข

- `gen_subtitle/gradio/app.py`
  - ปรับปรุง `format_quota_display()`
  - เพิ่ม `refresh_quota()`
  - ปรับปรุง `login()`
  - ปรับปรุง `upload_video()`
  - เพิ่มปุ่ม refresh quota ใน UI
  - เพิ่ม event handler

## หมายเหตุ

- ข้อมูล usage จะถูกเก็บใน session state (`session.usage`)
- Backend API จะคำนวณและส่งข้อมูล usage กลับมาทุกครั้งที่ upload
- ผู้ใช้สามารถ refresh ข้อมูลได้ตลอดเวลาโดยกดปุ่ม
- Session จะถูก track ตาม `session_id` ที่ backend สร้างให้
