# คู่มือการทดสอบ File Size Limit

## วิธีทดสอบการแก้ไข

### 1. เริ่มต้นระบบ

```bash
# Terminal 1: เริ่ม Backend
cd gen_subtitle
./start-backend.sh

# Terminal 2: เริ่ม Gradio Frontend
./start-gradio.sh
```

### 2. ทดสอบกรณีต่างๆ

#### กรณีที่ 1: ไฟล์ขนาดปกติ (< 500 MB)
1. เปิด browser ไปที่ http://localhost:7860
2. Login เข้าระบบ
3. ตรวจสอบว่าเห็นข้อความ:
   ```
   ข้อจำกัด:
   - 📏 ขนาดไฟล์สูงสุด: 500 MB
   - ⏱️ ความยาววิดีโอสูงสุด: 10 นาที
   - 📁 รองรับไฟล์: MP4, MOV, AVI, MKV, WMV
   ```
4. Upload ไฟล์วิดีโอขนาดเล็ก (เช่น 50 MB)
5. **ผลลัพธ์ที่คาดหวัง:** Upload สำเร็จ แสดงข้อความ "✅ อัปโหลดสำเร็จ!"

#### กรณีที่ 2: ไฟล์ขนาดใหญ่เกิน (> 500 MB)
1. เตรียมไฟล์วิดีโอที่มีขนาดมากกว่า 500 MB
2. พยายาม upload ไฟล์นั้น
3. **ผลลัพธ์ที่คาดหวัง:** แสดงข้อความแจ้งเตือนทันที:
   ```
   ❌ ไฟล์ใหญ่เกินไป!
   
   💾 ขนาดไฟล์ของคุณ: XXX.XX MB
   📏 ขนาดสูงสุดที่อนุญาต: 500 MB
   
   ⚠️ กรุณาเลือกไฟล์ที่มีขนาดเล็กกว่า 500 MB
   ```
4. **ไม่ควรเห็น:** HTTP 500 Error หรือ error ที่ไม่ชัดเจน

#### กรณีที่ 3: Backend ไม่ทำงาน
1. ปิด backend server
2. พยายาม upload ไฟล์
3. **ผลลัพธ์ที่คาดหวัง:** แสดงข้อความ:
   ```
   ❌ เกิดข้อผิดพลาดในการเชื่อมต่อ
   
   ไม่สามารถเชื่อมต่อกับ backend server ได้
   กรุณาตรวจสอบว่า backend กำลังทำงานอยู่
   ```

### 3. สร้างไฟล์ทดสอบ (ถ้าไม่มีไฟล์ใหญ่)

#### macOS/Linux:
```bash
# สร้างไฟล์ทดสอบขนาด 600 MB
dd if=/dev/zero of=test_large_file.mp4 bs=1m count=600

# สร้างไฟล์ทดสอบขนาด 50 MB
dd if=/dev/zero of=test_small_file.mp4 bs=1m count=50
```

**หมายเหตุ:** ไฟล์ที่สร้างด้วยวิธีนี้จะไม่ใช่วิดีโอจริง แต่ใช้ทดสอบ file size validation ได้

### 4. ตรวจสอบ Logs

#### Backend Logs:
```bash
# ดู logs ของ backend
tail -f backend/logs/app.log  # ถ้ามี logging
```

#### Gradio Logs:
- ดูใน terminal ที่รัน Gradio
- ตรวจสอบว่าไม่มี traceback หรือ error

### 5. Checklist การทดสอบ

- [ ] เห็นข้อจำกัดของระบบในหน้า Upload
- [ ] Upload ไฟล์ขนาดปกติสำเร็จ
- [ ] Upload ไฟล์ขนาดใหญ่แสดง error ที่ชัดเจน
- [ ] ไม่เกิด HTTP 500 Error
- [ ] Error message เป็นภาษาไทยและอ่านง่าย
- [ ] แสดงขนาดไฟล์ที่ upload และ limit ที่อนุญาต

## การปรับแต่ง Limits

หากต้องการเปลี่ยน file size limit:

1. แก้ไขไฟล์ `backend/config/limits.json`:
```json
{
  "maxVideos": 10,
  "maxDurationMinutes": 10,
  "maxFileSizeMB": 1000,  // เปลี่ยนเป็น 1000 MB (1 GB)
  ...
}
```

2. Restart backend server
3. ทดสอบอีกครั้ง

## ปัญหาที่อาจพบและวิธีแก้

### ปัญหา: ยังเห็น HTTP 500 Error
**วิธีแก้:**
1. ตรวจสอบว่า Gradio app ใช้ code ใหม่แล้ว
2. Restart Gradio server
3. Clear browser cache

### ปัญหา: ไม่เห็นข้อความ limits
**วิธีแก้:**
1. ตรวจสอบว่า backend ทำงานอยู่
2. ตรวจสอบว่า login สำเร็จ
3. ตรวจสอบ API endpoint `/limits` ทำงานปกติ

### ปัญหา: File size validation ไม่ทำงาน
**วิธีแก้:**
1. ตรวจสอบว่าไฟล์มีขนาดจริงเกิน limit
2. ตรวจสอบว่า `os.path.getsize()` ทำงานปกติ
3. ดู console logs ใน browser (F12)
